// TODO: make the px value adjustable on the fly. Right now some values are only set on init, so changing px will only work properly on init
// TODO: Scale the px on window resize

px = 1; // Scale of a "pixel"

let gameAspectRatio = 4 / 3;
let windowAspectRatio = window.innerWidth / window.innerHeight;
